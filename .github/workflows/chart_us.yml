name: 미장 백테스트 차트 생성

on:
  workflow_dispatch:

jobs:
  chart:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 한글 폰트 설치
        run: |
          sudo apt-get install -y fonts-nanum
          fc-cache -fv

      - name: 패키지 설치
        run: pip install pandas numpy matplotlib requests

      - name: matplotlib 캐시 초기화
        run: python3 -c "import matplotlib.font_manager; matplotlib.font_manager._load_fontmanager(try_read_cache=False)"

      - name: 차트 생성
        env:
          MASSIVE_TOKEN: ${{ secrets.MASSIVE_TOKEN }}
        run: python chart_backtest_us.py

      - name: PDF 업로드
        uses: actions/upload-artifact@v4
        with:
          name: backtest-charts-us
          path: backtest_charts_us.pdf
          retention-days: 30